# Requirements for Streamlit Cloud deployment
# Force rebuild: 2026-01-03-v11-minimal-test
# For local development, use: pip install -e ".[all]"

# Note: NOT using pip install . here as it requires build dependencies
# app.py adds src/ to sys.path for imports

# Core
numpy>=1.24.0
pandas>=2.0.0
xarray>=2023.1.0
pydantic>=2.0.0
scipy>=1.10.0
pyarrow>=14.0.0
# Note: rasterio removed - requires GDAL, not needed for dashboard

# Dashboard
streamlit>=1.30.0
pydeck>=0.9.0
streamlit-folium>=0.15.0
streamlit-local-storage>=0.0.25
branca>=0.6.0

# Cache
duckdb>=0.9.0

# Data fetch (requests used by some components)
requests>=2.28.0

# Note: herbie-data removed - requires eccodes system library
# Dashboard reads from DuckDB cache, doesn't need herbie at runtime
# Background refresh script runs locally where herbie is available
